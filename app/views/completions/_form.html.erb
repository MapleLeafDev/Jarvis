<%= form_for(@completion) do |f| %>
  <% if @completion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@completion.errors.count, "error") %> prohibited this completion from being saved:</h2>

      <ul>
      <% @completion.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br />
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :task_id %><br />
    <%= f.number_field :task_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
