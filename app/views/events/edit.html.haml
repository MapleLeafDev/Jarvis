.span6
  .well
    %h2.lead{align: "center"}
      Editing:
      %em=@event.name
    =render 'form'
  %p
    = link_to 'Back', :back, class: "btn btn-small"
    = link_to ' Delete', @event, confirm: 'Are you sure?', method: :delete, class: "i icon-remove btn btn-small"