%div{align: "center"}
  %table.table
    %tr
      %td.col-md-1{rowspan: "2"}
        = image_tag(gravatar_for(@family, 40))
      %td
        .lead
          .pull-right
            = link_to "", @access_url, class: "glyphicon glyphicon-new-window btn btn-default"
            -if current_user.parent?
              =link_to "", multi_families_path(id: @family.id), remote: true, class: "glyphicon glyphicon-cog btn btn-default"
          = t('views.blank_family', family: @family.name)
    %tr
      %td

-@family.users.each do |user|
  =render partial: 'card_m', locals: { user: user }

= render partial: 'users/tasks', locals: { family: @family, user: nil, tasks: @family.tasks, section: t('views.tasks') }