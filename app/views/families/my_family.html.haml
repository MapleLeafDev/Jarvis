

.span8{align: "center"}
  %h2.lead
    = @family.name
    Family

  -@users.each do |user|
    = link_to (image_tag(gravatar_for(user.email == nil || user.email == "" ? user.name : user.email))), user_path(user)
    %div
      = form_tag sessions_path do
        .field
          = label_tag :pin, 'PIN'
          = password_field_tag :pin, '', class: "input-small", :maxlength => 4
        .field
          = hidden_field_tag :id, params[:id], value: user.id
        .actions
          = submit_tag "Log In", class: "btn"
    %br